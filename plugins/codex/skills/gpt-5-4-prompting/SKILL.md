---
name: gpt-5-4-prompting
description: Legacy compatibility entrypoint; use codex-cli-runtime for delegation
user-invocable: false
---

# Legacy prompting entrypoint

The forwarding agent no longer loads this skill. The active contract is [codex-cli-runtime](../codex-cli-runtime/SKILL.md).
Use the supplied objective, scope and acceptance criteria without imposing legacy GPT-5.4 prompt templates.
The reference files are retained for backwards compatibility, not active runtime instructions.
